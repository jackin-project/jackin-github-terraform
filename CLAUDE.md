# CLAUDE.md

See [AGENTS.md](AGENTS.md) for shared agent instructions.

Security-critical repository. Before every commit, run the pre-commit checks in AGENTS.md. Rotate any credential that appears in a commit — even a deleted one.

This repository uses `main` as its primary branch.
