schema = 2
[generator]
repository = "jackin-project/jackin-github-terraform"
revision = "eed474c4a1d9b071fd1b5de00c769c8997398e5a"
# Un-required posture: the live ruleset gates on nothing for this repo, so the
# aggregate stays advisory (no ci-required job, no declared contexts).
[policy]
ci_required = false
[workflow]
providers = ["github-hosted"]
automatic_providers = ["github-hosted"]
default_branch = "main"
[workflow.selectors.github-hosted]
runs_on = ["ubuntu-24.04"]
